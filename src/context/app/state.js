import data from "../../mock";

const state = {
  pro: true,
  folders: data.folders,
  apps: data.apps,
  appsDict: data.appsDict,
  folderOpenName: null,
  folderOpen: null,
};

export default state;
